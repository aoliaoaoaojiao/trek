# Compatibility: model

This directory is kept for old path compatibility only.

Current status:
- Real model implementation/entry moved to `internal/engine/decision` and shared graph core in `internal/engine/decision/shared/model`.
- This directory should contain alias/forward functions only.

Do not add new graph/model logic here.
