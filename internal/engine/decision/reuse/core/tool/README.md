# Compatibility: tool

This directory is kept for old path compatibility only.

Current status:
- Real utility implementation moved to `internal/engine/decision/shared/tool`.
- This directory should contain alias/forward functions only.

Do not add new utility logic here.
